Fix three broken purchase flows — the free plot claim works perfectly, use that exact same NFT transfer logic for all other purchases:
1 — Paid Plot Purchase Not Delivering NFT
	•	The free plot claim flow works end to end — find that exact code path and trace what it does differently from the paid purchase flow
	•	The paid purchase receives the ALGO payment on-chain but never triggers the NFT transfer
	•	Find where the server verifies the incoming payment transaction and identify why it’s failing silently
	•	Check if the payment verification is looking for the wrong amount, wrong sender, wrong receiver, or wrong asset
	•	Fix the paid purchase flow to mirror the free claim flow exactly — payment verified → opt-in confirmed → NFT transferred → DB updated
	•	Test with a 0.1 ALGO purchase and confirm NFT arrives in wallet
2 — Commander Purchase — No ALGO Payment Verification
	•	Commander purchase is failing with “no ALGO payment” or “cannot verify payment”
	•	Find the commander purchase endpoint and show the current verification logic
	•	Add proper ALGO payment verification matching the same pattern as the working free plot claim
	•	Commander purchase flow must be: player sends ALGO + FRNTR atomic group tx → server verifies both legs on-chain → commander NFT transferred → DB updated
	•	If commander has no ALGO price set yet, set a testnet price of 0.5 ALGO
	•	Make sure opt-in to commander ASA is triggered before transfer attempt
3 — Universal Fix — Payment Verification Pattern
	•	Extract the working free plot claim verification logic into a reusable function
	•	Apply that same verified transfer function to: paid plot purchases, commander purchases, any future NFT purchases
	•	Every paid purchase must: verify payment on-chain → confirm opt-in → transfer NFT → update DB — in that exact order
	•	If any step fails, log the exact failure reason and return a clear error to the frontend
	•	Never silently fail — if the NFT transfer fails after payment, log it as a critical error and flag it for admin review in the dashboard
Show me the free plot claim code and the paid purchase code side by side before making any changes.​​​​​​​​​​​​​​​​
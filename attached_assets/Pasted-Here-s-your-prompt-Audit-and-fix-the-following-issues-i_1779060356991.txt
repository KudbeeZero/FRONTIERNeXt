Here’s your prompt:

Audit and fix the following issues in order:
1 — Land NFT Metadata + Images
	•	Fix the static file server so /nft/biomes/{biome}.png serves correctly from client/public/nft/biomes/
	•	Confirm every biome image (volcanic, desert, forest, mountain, plains, swamp, tundra, water) loads publicly
	•	Add #arc3 suffix to all NFT metadata URLs in the minting code
	•	Create and run scripts/update-asset-urls.ts to update all already-minted Land NFT ASAs on testnet from localhost URLs to ${PUBLIC\_BASE\_URL}/nft/metadata/${plotId}#arc3
	•	Fix purchasePriceAlgo showing as null in metadata — wire the real price in
2 — Commander NFTs + ALGO Payment
	•	Audit the commander minting and transfer flow
	•	Confirm commander NFT metadata is ARC-3 compliant with working image URLs
	•	Add ALGO payment requirement to commander purchases — currently no ALGO leg exists
	•	Commander purchase must be an atomic group transaction: ALGO payment + FRNTR deduction together or both fail
	•	Wire commander traits into battle stat calculations if not already connected
3 — AI Faction Economy (DB Simulation)
	•	AI factions currently drain resources but accumulate nothing — fix this
	•	Each faction wallet gets a simulated DB balance for FRNTR and resources (iron, fuel, crystal)
	•	Every mine action credits the faction’s DB balance with the mined resources
	•	Factions spend resources on upgrades and attacks — deduct from DB balance
	•	Add faction treasury display to the admin dashboard showing each faction’s current simulated holdings
	•	Flag all simulated balances clearly as SIMULATED so they’re easy to swap to on-chain later
4 — AI vs AI Combat
	•	AI factions cannot currently attack each other — fix this
	•	NEXUS-7, KRONOS, VANGUARD, SPECTRE should evaluate each other as valid attack targets
	•	Attack decisions should be based on real plot stats — factions target weakly defended enemy plots
	•	VANGUARD (raider) should prioritize attacking other factions over players
	•	KRONOS (defensive) should retaliate when attacked by another faction
	•	Log all AI vs AI battles in the battle history with faction names as attacker and defender
	•	Show AI vs AI battles in the War Room live feed
5 — Battle Resolution Fixes
	•	resolveBattles query taking 12-19 seconds — convert to single bulk operation, add 15 second statement timeout
	•	runAITurn taking up to 298 seconds — convert to bulk operation, add indexes on all WHERE clause columns, add 10 second hard timeout per tick
	•	Stagger background task startup: AI at 0s, battles at 5s, markets at 10s, transfers at 15s, orbital at 20s
	•	Add statement_timeout = 15000 at the DB pool level so no query ever holds a connection longer than 15 seconds
6 — Smart Contract Audit
	•	Review all ASA creation parameters for Land NFTs and Commander NFTs
	•	Confirm clawback, freeze, and manager addresses are all set to admin wallet
	•	Confirm total = 1 and decimals = 0 for all NFTs
	•	Flag any ASA missing required fields
	•	Confirm opt-in is triggered before every NFT transfer attempt
Do each section in order. Report findings before making changes in each section. Do not break anything that is currently working.​​​​​​​​​​​​​​​​
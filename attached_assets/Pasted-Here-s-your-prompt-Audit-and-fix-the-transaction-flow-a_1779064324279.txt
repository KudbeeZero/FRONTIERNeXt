Here’s your prompt:

Audit and fix the transaction flow and UI cleanup in this order:
1 — Transaction Batching Audit
	•	Review the entire transaction building pipeline for plot purchases and commander mints
	•	Show the current atomic group transaction setup — confirm both legs (ALGO + FRNTR) are being built correctly with proper amt values in microALGO
	•	Confirm assignGroupID is being called on both transactions before signing
	•	Confirm both transactions are submitted together as a group not separately
	•	Confirm the server verifies both legs are confirmed on-chain before doing anything in DB
	•	Fix any step that is broken or missing
2 — Remove Free Plot Claim Entirely
	•	Delete the free plot claim feature completely — remove the endpoint, the frontend button, the DB logic, and any tutorial references to it
	•	Remove any welcome/onboarding flow that gives away a free plot
	•	The only way to get a plot is to purchase it
	•	Make sure removing this doesn’t break anything else
3 — Remove All Claim Notification Buttons Except One
Remove the claim/opt-in notification button from ALL of these locations:
	•	Bottom left notification popup on the main screen — remove entirely
	•	Commander section top bar claim button — remove entirely
	•	Any other floating notifications or banners that prompt claiming
Keep the claim button ONLY in these two places:
	•	The plot card itself — both mobile and desktop versions
	•	The individual plot detail page
4 — Purchase Flow — One Clean Path
The only purchase flow that should exist:
	•	Player finds a plot on the globe or plot list
	•	Clicks the plot card — sees plot details and a single Purchase button
	•	Wallet prompted — transaction built with correct amt (100000 microALGO for plots)
	•	Payment confirmed on-chain
	•	Opt-in triggered automatically — player should NOT have to manually opt in
	•	NFT transferred to player wallet automatically
	•	Plot card updates to show owned status
	•	No extra steps, no pop-ups, no secondary claim buttons
5 — Commander Purchase Flow
	•	Commander purchase button lives only on the commander page
	•	Transaction built with correct amt (500000 microALGO for sentinel tier)
	•	Receiver must be admin treasury wallet not player wallet
	•	Opt-in to commander ASA triggered automatically before transfer
	•	Commander appears in player profile after confirmed
6 — Verify After Changes
	•	[ ] Plot purchase completes end to end with no manual opt-in step
	•	[ ] Commander mint completes end to end
	•	[ ] No claim buttons exist outside the plot card
	•	[ ] Free plot claim is completely gone from codebase
	•	[ ] Transaction group has correct amt on both legs
	•	[ ] No 400 errors on purchase or mint
Show what you’re removing before deleting anything.​​​​​​​​​​​​​​​​
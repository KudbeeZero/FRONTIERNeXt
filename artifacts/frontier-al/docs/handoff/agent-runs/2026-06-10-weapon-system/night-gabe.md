# Night Shift — Gabe (Documentation & Compliance)

## Gabe — Night Shift Report
**Focus**: Weapon system PR #9 documentation vs code on `origin/main` (merge commit `d9bbab5`): `docs/WEAPON_SYSTEM.md`, `session-notes/2026-06-10-weapon-system-phase1.md`, `shared/weapon-economy.ts` vs `TOKENOMICS.md`/`ECONOMICS.md`, `docs/PROJECT MEMORY.md` §3/§4/§7, `GAME_MANUAL.md`/`STRATEGY_GUIDE.md` coverage, JSDoc on `shared/weapons/*`. NOTE: the working tree was mutated mid-review by another agent (checked out `9e7b8b9`, which lacks the weapon files); all verification below was done against `origin/main` via `git show`/`git grep`, not the live tree.

**Findings Table**

| ID | Severity | Category | File(s) | Description | Impact | Suggested Fix | Test Idea |
|---|---|---|---|---|---|---|---|
| G1 | High | Stale doc / security posture | docs/WEAPON_SYSTEM.md:77 | Documents WS event `weapon_battery`, but audit commit `33e4dd6` deliberately removed that broadcast (fog-of-war leak of battery position/type/ammo). `git grep weapon_battery` on origin/main code = 0 hits; routes.ts ~2150 has an explicit "intentionally NOT broadcast" comment. | Client devs build against a nonexistent event; doc contradicts the security fix the same PR shipped. | Rewrite line 77 (see Code Suggestions). | CI doc-lint: grep docs for WS event names and assert each appears in `broadcastRaw` call sites. |
| G2 | High | Tokenomics gap | TOKENOMICS.md:63-75 ("Where FRNTR Goes (Sinks)"), ECONOMICS.md:121 ("Sinks") | Neither sink table mentions weapon costs. `shared/weapon-economy.ts` defines fire (4–230 FRNTR per spec), unlock (6× fire), upgrade (3× fire × tier), deploy-defense (4× fire) — all true burns: `db.ts:863 spendFrontier` decrements `frntrBalanceMicro` AND increments `totalFrontierBurned`. Merged to main, so the "deflationary heartbeat" tables are materially incomplete. | Players/partners mis-model the economy; weapon sinks invisible in the canonical tokenomics doc despite feeding `totalBurned` in `/api/economics`. | Add weapon rows to both sink tables (see Code Suggestions); note the 0.25 testing-mode discount (`WEAPON_TEST_DISCOUNT`). | Unit test asserting every `spendFrontier` call site has a documented sink row (table-driven list). |
| G3 | Med | Stale project memory | docs/PROJECT MEMORY.md:7,59-105,149 | §3 (Done) and §4 (In Flight) do not reflect the PR #9 merge (`d9bbab5` is origin/main HEAD); "Last updated: 2026-06-07"; §7 says "Routes: 84" but origin/main routes.ts has 100 `app.get/post/...` registrations. | Next session re-derives or double-builds; violates the file's own maintenance rule. | Add weapon system to §3 with the one-time `pnpm run db:push` op; update §7 route count to 100; bump the date. | Pre-merge checklist item: PR template box "PROJECT MEMORY §3/§4 updated". |
| G4 | Med | Missing player docs | GAME_MANUAL.md, STRATEGY_GUIDE.md | No weapon/armory coverage anywhere (only flavor text "Siege-tier weaponry" GAME_MANUAL.md:121). The system is player-facing: `/armory` route, attribute builds, badges, FRNTR costs, NFT minting. | Players hit /armory with zero manual/strategy guidance; costly burns undocumented for users. | GAME_MANUAL: add "Armory & Weapons" section (build → badges → unlock → fire/defend → costs → NFT mint). STRATEGY_GUIDE: tension pairs (firepower⟷logistics, range⟷guidance, interception⟷firepower), soft cap 14/budget 60, concealed-battery defense play. | Doc review pass after any new player-facing route is added. |
| G5 | Low | Internal contradiction | session-notes/2026-06-10-weapon-system-phase1.md:53 vs :85-87 | §4 says routes `broadcastRaw({type:"weapon_engagement"|"weapon_battery"})`; §6 of the same file records the `weapon_battery` broadcast's removal. Chronological note, but §4 reads as current state. | Readers skimming §4 get the pre-audit behavior. | Annotate line 53: "(weapon_battery later removed — see §6 audit pass)". | n/a (one-line edit). |
| G6 | Low | Inaccurate doc | docs/WEAPON_SYSTEM.md:27 | "Bronze→Gold→Hall-of-Fame" omits Silver. `badges.ts` `BADGE_TIER_ORDER` = none/bronze/silver/gold/hall_of_fame (its JSDoc and the session note both say Bronze/Silver/Gold/HoF). | Minor; misstates progression granularity. | "Bronze→Silver→Gold→Hall-of-Fame". | Doc-lint tier list against `BADGE_TIER_ORDER`. |
| G7 | Low | Imprecise wording | docs/WEAPON_SYSTEM.md:32,75; shared/weapon-economy.ts:4 | (a) "cost tables (TEST/PROD)" — weapon-economy.ts is multiplier *functions* + flat `WEAPON_TEST_DISCOUNT=0.25`, not per-mode tables (economy-config.ts has real TEST/PROD constants; the file's own header overstates the mirroring). (b) Line 75 "(spec fields only)" — `/nft/metadata/weapon/:ownedWeaponId` also returns instance fields: `ownedWeaponId`, `upgradeTier`, `nftAssetId`. Param is `:ownedWeaponId`, not `:id`. | Misleads anyone tuning the economy or consuming the metadata. | Reword per Code Suggestions. | Snapshot test of metadata route response keys vs doc. |
| G8 | Low | Unverified claims | session-notes/...phase1.md:11,58,105,106 | Test counts (147→156→158 server tests) and "db:push not yet applied to the real DB" could NOT be verified — the shared working tree was on a non-weapon commit, so I did not run the suite; DB state is unobservable from the repo. Flagged, not assumed wrong. | Counts may drift after merge with globe E7-E9 work. | Re-run `pnpm run test:server` on a clean main checkout and update the note if off. | The Verify section already prescribes exactly this. |

**Key Insights**
- Verified ACCURATE (claim-by-claim vs origin/main): all 9 documented routes exist at routes.ts:2056-2218 with `assertPlayerOwnership` on every `/api/weapons/*` (incl. GET catalog via query playerId); catalog counts 12 missiles / 12 artillery / 8 anti-air / 6 defense; `TENSION_PAIRS`, soft cap 14, budget 60; `recomputeDerived` on every write; per-weapon `weaponMintInFlight` mint lock; `MAX_WEAPON_UPGRADE_TIER` enforced; `/armory` route wired in App.tsx; stat credits on `status !== "intercepted"` incl. defender `intercepts`; agent-memory's "+239 lines in routes.ts" matches the diff (240 added lines).
- The one systemic failure mode repeats agent-memory's L4 lesson: the durable doc (`WEAPON_SYSTEM.md`, committed `ae4196c`) was written BEFORE the audit commit (`33e4dd6`) and never re-synced — G1/G6/G7 all stem from that ordering. Suggest a rule: any audit/fix commit that touches behavior re-touches the durable doc in the same commit.
- Weapon spends are real burns (they increment `totalFrontierBurned`), so they already flow into the `/api/economics` "Burned" pool — the code is ahead of the tokenomics docs, not vice versa. Yes, they should appear in TOKENOMICS.md/ECONOMICS.md.
- Repo-state hazard for this multi-agent run: the checkout moved from `origin/pr/9` to `9e7b8b9` mid-review. Any agent reviewing via the working tree (not `git show origin/main:`) may have reviewed the wrong revision.
- JSDoc quality on `shared/weapons/*` is exemplary — all 10 sampled modules carry purposeful file-header docs (intent + design rationale + cross-refs) and commented constants. No action needed; worth citing as the house style.

**Code Suggestions**
- G1 — docs/WEAPON_SYSTEM.md:77, replace with:
  `WS events: \`weapon_engagement\` (launch/intercept/impact). Battery deployments are deliberately NOT broadcast — batteries are concealed intel (fog-of-war) and are revealed only when they intercept, via the engagement event.`
- G2 — TOKENOMICS.md sinks table, add rows (and mirror in ECONOMICS.md §Sinks):
  `| Weapon fire (per shot) | 4 – 230 (per spec \`costFrntr\`) |`
  `| Weapon unlock | 6× fire cost |`
  `| Weapon upgrade (per tier N) | 3× fire cost × N |`
  `| Missile-defense battery deploy | 4× fire cost |`
  plus footnote: "Testing mode scales weapon costs to 25% (`WEAPON_TEST_DISCOUNT`); weapon NFT mint costs only the 0.001 ALGO network fee."
- G6 — docs/WEAPON_SYSTEM.md:27: `badges.ts ... Bronze→Silver→Gold→Hall-of-Fame, computeBadges()`
- G7a — docs/WEAPON_SYSTEM.md:32: `shared/weapon-economy.ts fire/unlock/upgrade/deploy FRNTR cost functions (multipliers on each spec's costFrntr; 25% testing-mode discount)`
- G7b — docs/WEAPON_SYSTEM.md:75: `| GET | \`/nft/metadata/weapon/:ownedWeaponId\` | public ARC-3 metadata (spec + instance fields: upgradeTier, nftAssetId) |`
- G3 — docs/PROJECT MEMORY.md §3, append under a 2026-06-10 heading: `✅ Weapon system (PR #9, merge d9bbab5): shared/weapons/* catalog+progression+sim, weaponProfile jsonb (needs one-time pnpm run db:push), /api/weapons/* (9 routes), Armory UI at /armory, weapon NFT mint` — and §7: `Routes: **100** in routes.ts`.

**Confidence Score**: 8/10
(−1: could not run the test suite or check DB state on the mutating shared tree; −1: PR #9 reviewed via origin/main snapshot rather than a stable checkout.)
